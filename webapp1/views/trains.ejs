<!DOCTYPE html>
<html lang="en">
<% include header %>
<body>
	<% include navbar %>

	<div class="container">
		<div class="page-header">
			<h1>
				trains <small>What we aim for!</small>
			</h1>
		</div>
		<div class="panel panel-default">
			<div class="panel-heading">trains</div>
			<div class="panel-body">
				<p>Trains content</p>
				<strong><%= array %></strong>
						
					  <% for(var i=0; i<array['sync_contact_list'].length; i++) {%>
						 
						  
						 <tr>
						   <td><%= array['sync_contact_list'][i]['name'] %></td>
						   <td><%= array['sync_contact_list'][i]['number'] %></td>
						   <td><%= array['sync_contact_list'][i]['email'] %></td>
						 </tr>
						 <% } %>
			</div>
		</div>

		<hr>

		<% include footer %>
	</div>
	<!-- /container -->
	<script type="text/javascript">
		$(document).ready(function() {
			$('#trains').addClass("active");
		});
	</script>
	<!-- Bootstrap core JavaScript
    ================================================== -->
	<!-- Placed at the end of the document so the pages load faster -->
	<!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
	<script src="https://code.jquery.com/jquery.js"></script>
	<!-- Latest compiled and minified JavaScript -->
	<script
		src="//netdna.bootstrapcdn.com/bootstrap/3.0.3/js/bootstrap.min.js"></script>
</body>
</html>